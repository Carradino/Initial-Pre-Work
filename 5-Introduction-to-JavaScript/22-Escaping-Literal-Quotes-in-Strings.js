/* Backslash (\) allows you to use the same type of quote notation inside of a
string. Essentially, the \ tells JS to ignore the character that follows the
backslash. */
var myStr = "I am a \"double quoted\" string inside \"double quotes\"."; // Change this line
