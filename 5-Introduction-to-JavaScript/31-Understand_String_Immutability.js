/*Strings are immutable. They cannot be changed, a variable can be reassigned
a new string. But the old string can't be changed.*/
// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
