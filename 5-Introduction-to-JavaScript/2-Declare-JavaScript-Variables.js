/* There are seven data types in Javascript, number, string, undefined, null,
boolean, symbol and object. Variables store data, they can be made up of numbers
and letters and symbols $, _, but they can't start with numbers. var is how we
declare variables in Javascript, the declaration must end with a semi-colon.*/
// Example
var ourName;

// Declare myName below this line
var myName;
