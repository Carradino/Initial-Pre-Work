// Values can be passed to functions w/ arguments and values can be returned from functions with return.
// Example
function minusSeven(num) {
  return num - 7;
}

// Only change code below this line

function timesFive(digit){
  return digit * 5;
}

console.log(minusSeven(10));

console.log(timesFive(5));
