// Math.random(); can generate random decimals between 0(included) and 1(not included).
function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
