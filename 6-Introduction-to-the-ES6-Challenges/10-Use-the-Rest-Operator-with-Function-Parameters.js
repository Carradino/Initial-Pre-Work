// The rest operator ...args allows functions with a variable number of parameters.
const sum = (function(args) {
  "use strict";
  return function sum(...args) {
    return args.reduce((a, b) => a + b, 0);
  };
})();
console.log(sum(1, 2, 3)); // 6
